import 'package:flutter/material.dart';
class TemperGlassScreen extends StatelessWidget {
  const TemperGlassScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Temper Glass')), body: const Center(child: Text('Temper Glass coming soon')));
}